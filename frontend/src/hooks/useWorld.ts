import {useEffect,useState} from 'react'; import type {World} from '../types';
const fallback:World={mode:'LIVE',host_vehicle:{vehicle_id:'HOST',latitude:null,longitude:null,speed_mps:null,heading_deg:null,satellites:0,fix:false,accuracy_m:null},vehicles:[],system_status:{host_gps:'NO_FIX',phone_b01:'WAITING',backend:'DISCONNECTED',camera:'DISCONNECTED',imu:'DISCONNECTED',v2v:'DISCONNECTED'},scenario:null,demo_running:false};
export function useWorld(){const [world,setWorld]=useState<World>(fallback); const [connected,setConnected]=useState(false); useEffect(()=>{const proto=location.protocol==='https:'?'wss':'ws'; const ws=new WebSocket(`${proto}://${location.hostname}:8000/ws`); ws.onopen=()=>setConnected(true); ws.onclose=()=>setConnected(false); ws.onmessage=e=>setWorld(JSON.parse(e.data)); return()=>ws.close()},[]); return {world,connected};}
