import type {World} from '../types';
export function MapPanel({world}:{world:World}){const hasHost=world.host_vehicle.latitude!==null; return <div className="map-panel"><div className="map-grid"/><div className="map-label top">{world.mode==='LIVE'?'LIVE MAP':'DEMO MAP'} · {hasHost?`${world.host_vehicle.latitude!.toFixed(4)}, ${world.host_vehicle.longitude!.toFixed(4)}`:'WAITING FOR HOST GPS'}</div>{hasHost&&<div className="map-host"/>}<div className="map-legend"><span><i className="dot host"/> HOST</span><span><i className="dot target"/> VEHICLE</span><span><i className="dot danger"/> RISK ZONE</span></div>{world.vehicles.map(v=><div key={v.vehicle_id} className={`map-target ${v.risk.toLowerCase()}`} style={{left:`${50+v.relative_bearing_deg/180*35}%`,top:`${50-v.distance_m/100*35}%`}}>{v.vehicle_id}</div>)}</div>}
